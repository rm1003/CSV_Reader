#ifndef FUNC_FILTROS_H
#define FUNC_FILTROS_H
/*     Funcoes para a funcao 3     */

// Aqui compara se menor devolve 1. E usa no merge
int compara_menor(char *a, char *b);

// Aqui compara  se menor igual devolve 1
int compara_menor_igual(char *a, char *b);

// Aqui compara se maior devolve 1
int compara_maior(char *a, char *b);

// Aqui compara se maior igual devolve 1
int compara_maior_igual(char *a, char *b);

// Aqui compara se igual devolve 1
int igual(char *a, char *b);

// Aqui compara se diferente devolve 1
int diferente(char *a, char *b);

#endif